<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complete Liquid String Filters Demo</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            line-height: 1.6; 
            color: #333; 
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            padding: 40px;
            margin-top: 20px;
        }
        header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 3px solid #4a6fa5;
        }
        h1 {
            color: #2c3e50;
            font-size: 2.8rem;
            margin-bottom: 10px;
        }
        .subtitle {
            color: #7f8c8d;
            font-size: 1.2rem;
        }
        .filters-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 25px;
            margin-top: 30px;
        }
        .filter-card {
            background: #fff;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            padding: 20px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        .filter-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
            border-color: #4a6fa5;
        }
        .filter-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 5px;
            height: 100%;
            background: #4a6fa5;
        }
        .filter-title {
            color: #2c3e50;
            font-size: 1.3rem;
            margin-bottom: 15px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .filter-title .icon {
            font-size: 1.5rem;
            color: #4a6fa5;
        }
        .code-block {
            background: #2c3e50;
            color: #ecf0f1;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
            font-family: 'Courier New', monospace;
            font-size: 0.95rem;
            overflow-x: auto;
            border-left: 4px solid #e74c3c;
        }
        .result {
            background: #e8f4f8;
            padding: 15px;
            border-radius: 8px;
            margin: 10px 0;
            border-left: 4px solid #3498db;
        }
        .result-label {
            color: #3498db;
            font-weight: 600;
            margin-bottom: 5px;
            display: block;
        }
        .input-string {
            color: #27ae60;
            font-weight: 500;
        }
        .output-string {
            color: #e74c3c;
            font-weight: 600;
            font-size: 1.1rem;
        }
        .category {
            font-size: 1.8rem;
            color: #2c3e50;
            margin: 40px 0 20px 0;
            padding-bottom: 10px;
            border-bottom: 2px dashed #bdc3c7;
            display: flex;
            align-items: center;
            gap: 15px;
        }
        .category::before {
            content: '▶';
            color: #4a6fa5;
            font-size: 1.5rem;
        }
        .practical-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 15px;
            margin-top: 50px;
        }
        .practical-section h2 {
            text-align: center;
            margin-bottom: 30px;
            font-size: 2rem;
        }
        .practical-example {
            background: rgba(255,255,255,0.1);
            padding: 20px;
            border-radius: 10px;
            margin: 20px 0;
        }
        .note {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            color: #856404;
            padding: 15px;
            border-radius: 8px;
            margin-top: 30px;
            font-size: 0.95rem;
        }
        footer {
            text-align: center;
            margin-top: 50px;
            padding-top: 20px;
            border-top: 1px solid #eee;
            color: #7f8c8d;
            font-size: 0.9rem;
        }
        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }
            .filters-grid {
                grid-template-columns: 1fr;
            }
            h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>📝 Liquid String Filters Master Reference</h1>
            <p class="subtitle">Complete demonstration of all Liquid string manipulation filters</p>
        </header>
        {% comment %} 
            BASIC STRING VARIABLES FOR DEMONSTRATION 
        {% endcomment %}
        {% assign sample_string = "  Hello WORLD!  " %}
        {% assign html_string = "<p>Visit our <strong>website</strong> at example.com</p>" %}
        {% assign multiline_text = "First line
        Second line
        Third line" %}
        {% assign product_name = "Premium Coffee Mug - 12oz | Ceramic" %}
        {% assign url_string = "Hello World! How's it going?" %}
        {% assign secret_message = "mySecretMessage123" %}

        <div class="note">
            <strong>📌 Note:</strong> All examples use the original input strings shown above. Filters are applied from left to right when chained.
        </div>

        <!-- BASIC STRING MANIPULATION -->
        <h2 class="category">🔤 Basic String Manipulation</h2>
        <div class="filters-grid">
            <div class="filter-card">
                <div class="filter-title"><span class="icon">A</span> capitalize</div>
                <p>Capitalizes first character, downcases rest</p>
                <div class="code-block">{{ "{{ '" | append: sample_string | append: "' | capitalize }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ sample_string }}"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ sample_string | capitalize }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">⇧</span> upcase</div>
                <p>Converts all characters to uppercase</p>
                <div class="code-block">{{ "{{ 'hello liquid' | upcase }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"hello liquid"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "hello liquid" | upcase }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">⇩</span> downcase</div>
                <p>Converts all characters to lowercase</p>
                <div class="code-block">{{ "{{ '" | append: sample_string | append: "' | downcase }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ sample_string }}"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ sample_string | downcase }}</span>
                </div>
            </div>
        </div>

        <!-- TEXT TRANSFORMATION -->
        <h2 class="category">🔄 Text Transformation</h2>
        <div class="filters-grid">
            <div class="filter-card">
                <div class="filter-title"><span class="icon">✂️</span> strip</div>
                <p>Removes whitespace from both ends</p>
                <div class="code-block">{{ "{{ '" | append: sample_string | append: "' | strip }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ sample_string }}"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">"{{ sample_string | strip }}"</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">←</span> lstrip</div>
                <p>Removes whitespace from left side only</p>
                <div class="code-block">{{ "{{ '" | append: sample_string | append: "' | lstrip }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ sample_string }}"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">"{{ sample_string | lstrip }}"</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">→</span> rstrip</div>
                <p>Removes whitespace from right side only</p>
                <div class="code-block">{{ "{{ '" | append: sample_string | append: "' | rstrip }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ sample_string }}"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">"{{ sample_string | rstrip }}"</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">🔗</span> strip_html</div>
                <p>Removes all HTML tags</p>
                <div class="code-block">{{ "{{ '" | append: html_string | append: "' | strip_html }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">{{ html_string }}</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ html_string | strip_html }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">↩️</span> strip_newlines</div>
                <p>Removes all newline characters</p>
                {% capture multiline_example %}First line
Second line
Third line{% endcapture %}
                <div class="code-block">{{ "{{ multiline_text | strip_newlines }}" }}</div>
                <div class="result">
                    <span class="result-label">Input (3 lines):</span><br>
                    <span class="input-string">{{ multiline_text }}</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ multiline_text | strip_newlines }}</span>
                </div>
            </div>
        </div>

        <!-- STRING FORMATTING -->
        <h2 class="category">🎨 String Formatting</h2>
        <div class="filters-grid">
            <div class="filter-card">
                <div class="filter-title"><span class="icon">⏪</span> prepend</div>
                <p>Adds string to beginning</p>
                <div class="code-block">{{ "{{ 'World' | prepend: 'Hello ' }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"World"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "World" | prepend: "Hello " }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">⏩</span> append</div>
                <p>Adds string to end</p>
                <div class="code-block">{{ "{{ 'Hello' | append: ' World' }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"Hello"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "Hello" | append: " World" }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">🔄</span> replace</div>
                <p>Replaces all occurrences</p>
                <div class="code-block">{{ "{{ 'banana' | replace: 'na', 'no' }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"banana"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "banana" | replace: "na", "no" }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">1️⃣</span> replace_first</div>
                <p>Replaces first occurrence only</p>
                <div class="code-block">{{ "{{ 'banana' | replace_first: 'na', 'no' }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"banana"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "banana" | replace_first: "na", "no" }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">❌</span> remove</div>
                <p>Removes all occurrences</p>
                <div class="code-block">{{ "{{ 'Hello World' | remove: 'lo' }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"Hello World"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "Hello World" | remove: "lo" }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">1️⃣❌</span> remove_first</div>
                <p>Removes first occurrence only</p>
                <div class="code-block">{{ "{{ 'banana' | remove_first: 'na' }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"banana"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "banana" | remove_first: "na" }}</span>
                </div>
            </div>
        </div>

        <!-- TEXT TRUNCATION -->
        <h2 class="category">📏 Text Truncation</h2>
        <div class="filters-grid">
            <div class="filter-card">
                <div class="filter-title"><span class="icon">✂️</span> truncate</div>
                <p>Truncates to specified character length</p>
                <div class="code-block">{{ "{{ 'The quick brown fox jumps' | truncate: 15 }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"The quick brown fox jumps"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "The quick brown fox jumps" | truncate: 15 }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">📝</span> truncatewords</div>
                <p>Truncates after specified word count</p>
                <div class="code-block">{{ "{{ 'The quick brown fox jumps over the lazy dog' | truncatewords: 4 }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"The quick brown fox jumps over the lazy dog"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "The quick brown fox jumps over the lazy dog" | truncatewords: 4 }}</span>
                </div>
            </div>
        </div>

        <!-- ENCODING & ESCAPING -->
        <h2 class="category">🔐 Encoding & Escaping</h2>
        <div class="filters-grid">
            <div class="filter-card">
                <div class="filter-title"><span class="icon">⚠️</span> escape</div>
                <p>HTML-escapes a string</p>
                <div class="code-block">{{ "{{ '<p>Hello & \"World\"</p>' | escape }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">{{ '<p>Hello & "World"</p>' }}</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ '<p>Hello & "World"</p>' | escape }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">1️⃣⚠️</span> escape_once</div>
                <p>Escapes without changing existing entities</p>
                <div class="code-block">{{ "{{ '&lt;p&gt;Hello & \"World\"&lt;/p&gt;' | escape_once }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">{{ '&lt;p&gt;Hello & "World"&lt;/p&gt;' }}</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ '&lt;p&gt;Hello & "World"&lt;/p&gt;' | escape_once }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">🌐</span> url_encode</div>
                <p>Converts to URL-safe encoding</p>
                <div class="code-block">{{ "{{ '" | append: url_string | append: "' | url_encode }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ url_string }}"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ url_string | url_encode }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">🔓</span> url_decode</div>
                <p>Decodes URL-encoded string</p>
                <div class="code-block">{{ "{{ 'Hello%20World%21%20How%27s%20it%20going%3F' | url_decode }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"Hello%20World%21%20How%27s%20it%20going%3F"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "Hello%20World%21%20How%27s%20it%20going%3F" | url_decode }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">📄</span> json</div>
                <p>Converts to JSON format</p>
                <div class="code-block">{{ "{{ 'Hello \"World\"' | json }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"Hello \"World\""</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ 'Hello "World"' | json }}</span>
                </div>
            </div>
        </div>

        <!-- STRING INSPECTION & EXTRACTION -->
        <h2 class="category">🔍 String Inspection & Extraction</h2>
        <div class="filters-grid">
            <div class="filter-card">
                <div class="filter-title"><span class="icon">📏</span> size</div>
                <p>Returns number of characters</p>
                <div class="code-block">{{ "{{ 'Hello World' | size }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"Hello World"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "Hello World" | size }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">✂️</span> slice</div>
                <p>Extracts substring by position and length</p>
                <div class="code-block">{{ "{{ 'Hello World' | slice: 6, 5 }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"Hello World"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "Hello World" | slice: 6, 5 }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">🎪</span> split</div>
                <p>Splits string into array using delimiter</p>
                {% assign colors = "red,green,blue,yellow" | split: "," %}
                <div class="code-block">{{ "{% assign colors = 'red,green,blue,yellow' | split: ',' %}" }}</div>
                <div class="result">
                    <span class="result-label">Array elements:</span><br>
                    {% for color in colors %}
                    <span class="output-string">{{ color }}</span>{% unless forloop.last %}, {% endunless %}
                    {% endfor %}
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">⏮️</span> first</div>
                <p>Gets first character</p>
                <div class="code-block">{{ "{{ 'Hello' | first }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"Hello"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "Hello" | first }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">⏭️</span> last</div>
                <p>Gets last character</p>
                <div class="code-block">{{ "{{ 'Hello' | last }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"Hello"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "Hello" | last }}</span>
                </div>
            </div>
        </div>

        <!-- SEARCH & PATTERN MATCHING -->
        <h2 class="category">🔎 Search & Pattern Matching</h2>
        <div class="filters-grid">
            <div class="filter-card">
                <div class="filter-title"><span class="icon">🔍</span> contains</div>
                <p>Checks if string contains substring</p>
                <div class="code-block">{{ "{{ 'Hello World' | contains: 'World' }}" }}</div>
                <div class="result">
                    <span class="result-label">Check:</span>
                    <span class="input-string">Does "Hello World" contain "World"?</span><br>
                    <span class="result-label">Result:</span>
                    <span class="output-string">{{ "Hello World" | contains: "World" }}</span>
                </div>
            </div>
        </div>

        <!-- CASE & FORMAT MANIPULATION -->
        <h2 class="category">🔄 Case & Format Manipulation</h2>
        <div class="filters-grid">
            <div class="filter-card">
                <div class="filter-title"><span class="icon">🐫</span> camelcase</div>
                <p>Converts to camelCase</p>
                <div class="code-block">{{ "{{ 'hello_world_example' | camelcase }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"hello_world_example"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "hello_world_example" | camelcase }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">🔗</span> handleize</div>
                <p>Creates URL-friendly handle</p>
                <div class="code-block">{{ "{{ '" | append: product_name | append: "' | handleize }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ product_name }}"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ product_name | handleize }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">📦</span> pluralize</div>
                <p>Returns plural form based on count</p>
                <div class="code-block">{{ "{{ 'product' | pluralize: 3 }}" }}</div>
                <div class="result">
                    <span class="result-label">Singular:</span>
                    <span class="input-string">"product"</span><br>
                    <span class="result-label">Count:</span>
                    <span class="input-string">3</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "product" | pluralize: 3 }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">📦</span> singularize</div>
                <p>Returns singular form</p>
                <div class="code-block">{{ "{{ 'products' | singularize }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"products"</span><br>
                    <span class="result-label">Output:</span>
                    <span class="output-string">{{ "products" | singularize }}</span>
                </div>
            </div>
        </div>

        <!-- ADVANCED STRING OPERATIONS -->
        <h2 class="category">⚡ Advanced String Operations</h2>
        <div class="filters-grid">
            <div class="filter-card">
                <div class="filter-title"><span class="icon">↵</span> newline_to_br</div>
                <p>Replaces newlines with &lt;br&gt; tags</p>
                {% capture multiline_example %}Line 1
Line 2
Line 3{% endcapture %}
                <div class="code-block">{{ "{{ multiline_text | newline_to_br }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span><br>
                    <span class="input-string">{{ multiline_text }}</span><br>
                    <span class="result-label">Output:</span><br>
                    <span class="output-string">{{ multiline_text | newline_to_br }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">🔒</span> md5</div>
                <p>Creates MD5 hash</p>
                <div class="code-block">{{ "{{ '" | append: secret_message | append: "' | md5 }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ secret_message }}"</span><br>
                    <span class="result-label">MD5 Hash:</span>
                    <span class="output-string">{{ secret_message | md5 }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">🔐</span> sha1</div>
                <p>Creates SHA1 hash</p>
                <div class="code-block">{{ "{{ '" | append: secret_message | append: "' | sha1 }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ secret_message }}"</span><br>
                    <span class="result-label">SHA1 Hash:</span>
                    <span class="output-string">{{ secret_message | sha1 }}</span>
                </div>
            </div>

            <div class="filter-card">
                <div class="filter-title"><span class="icon">🔏</span> sha256</div>
                <p>Creates SHA256 hash</p>
                <div class="code-block">{{ "{{ '" | append: secret_message | append: "' | sha256 }}" }}</div>
                <div class="result">
                    <span class="result-label">Input:</span>
                    <span class="input-string">"{{ secret_message }}"</span><br>
                    <span class="result-label">SHA256 Hash:</span>
                    <span class="output-string">{{ secret_message | sha256 }}</span>
                </div>
            </div>
        </div>

        <!-- PRACTICAL EXAMPLES SECTION -->
        <div class="practical-section">
            <h2>💡 Practical Real-World Examples</h2>
            
            <div class="practical-example">
                <h3>Example 1: Dynamic Product URL Generation</h3>
                {% assign product_title = "Awesome T-Shirt | Size: XL | Color: Blue" %}
                <div class="code-block">
                    {% raw %}{% assign product_title = "Awesome T-Shirt | Size: XL | Color: Blue" %}
{{ product_title | handleize }}{% endraw %}
                </div>
                <div class="result">
                    <span class="result-label">Generated URL Slug:</span>
                    <span class="output-string">{{ product_title | handleize }}</span>
                </div>
            </div>

            <div class="practical-example">
                <h3>Example 2: Clean User Input & Format</h3>
                {% assign user_input = "  JOHN DOE  " %}
                <div class="code-block">
                    {% raw %}{% assign user_input = "  JOHN DOE  " %}
{{ user_input | strip | downcase | capitalize }}{% endraw %}
                </div>
                <div class="result">
                    <span class="result-label">Formatted Name:</span>
                    <span class="output-string">{{ user_input | strip | downcase | capitalize }}</span>
                </div>
            </div>

            <div class="practical-example">
                <h3>Example 3: Create SEO-friendly Meta Description</h3>
                {% assign long_description = "Discover our amazing collection of premium products that will transform your daily routine with innovative features and exceptional quality guaranteed to satisfy." %}
                <div class="code-block">
                    {% raw %}{{ long_description | strip_html | truncatewords: 15 }}{% endraw %}
                </div>
                <div class="result">
                    <span class="result-label">Meta Description:</span>
                    <span class="output-string">{{ long_description | strip_html | truncatewords: 15 }}</span>
                </div>
            </div>

            <div class="practical-example">
                <h3>Example 4: Generate Secure Download Links</h3>
                {% assign filename = "confidential_report.pdf" %}
                {% assign secret_key = "mySecretKey2024" %}
                <div class="code-block">
                    {% raw %}{% assign filename = "confidential_report.pdf" %}
{% assign secret_key = "mySecretKey2024" %}
{{ filename | hmac_sha256: secret_key }}{% endraw %}
                </div>
                <div class="result">
                    <span class="result-label">Secure Token:</span>
                    <span class="output-string">{{ filename | hmac_sha256: secret_key | slice: 0, 16 }}</span>
                </div>
            </div>
        </div>

        <!-- FILTER CHAINING DEMO -->
        <div style="background: #f8f9fa; padding: 25px; border-radius: 10px; margin-top: 40px;">
            <h3>🔗 Filter Chaining Demonstration</h3>
            <p>Filters are applied from left to right:</p>
            {% assign messy_string = "  <p>HELLO <strong>WORLD</strong> USERS!</p>  " %}
            <div class="code-block">
                {% raw %}{% assign messy_string = "  <p>HELLO <strong>WORLD</strong> USERS!</p>  " %}
{{ messy_string | strip | strip_html | downcase | capitalize | append: " Welcome!" }}{% endraw %}
            </div>
            <div class="result">
                <span class="result-label">Step-by-step transformation:</span><br>
                1. <strong>strip:</strong> "{{ messy_string | strip }}"<br>
                2. <strong>strip_html:</strong> "{{ messy_string | strip | strip_html }}"<br>
                3. <strong>downcase:</strong> "{{ messy_string | strip | strip_html | downcase }}"<br>
                4. <strong>capitalize:</strong> "{{ messy_string | strip | strip_html | downcase | capitalize }}"<br>
                5. <strong>append:</strong> <span class="output-string">{{ messy_string | strip | strip_html | downcase | capitalize | append: " Welcome!" }}</span>
            </div>
        </div>

        <footer>
<p>📚 <strong>Complete Liquid String Filters Reference</strong> | Filters demonstrated: 30+ | Last updated: {{ "now" | date: